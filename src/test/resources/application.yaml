spring:
  mvc:
    hiddenmethod:
      filter:
        enabled: true
      #sql:
      #init:
      #mode: always
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
    defer-datasource-initialization: true